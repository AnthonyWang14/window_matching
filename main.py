from eval import *

if __name__ == '__main__':
    np.random.seed(1)
    # diff_type_number(dist_type=3)
    diff_density(dist_type=3)
    # diff_type_number(dist_type=3)
    # diff_density(dist_type=3)
    # diff_shift()